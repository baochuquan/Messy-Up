#include <stdio.h>
#include <vector>
using namespace std;

vector<vector<char> > ans;

void helper(vector<char>& gray, int pos){
    if(pos == n){
        gray.push_back(gray);
        return ;
    }
    for(int i = 0; i < )
}

int main(){
    vector<char> gray(n, 0);

}
